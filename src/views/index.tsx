import { defineComponent } from "vue";

const index =  defineComponent({
    setup () {
        const forKey = () =>{
            const list = ref([])
        }
        return () =>
            <div>
                <a href=""></a>
            </div>;
    }
})
export default index